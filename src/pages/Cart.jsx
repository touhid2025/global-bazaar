import React, { useEffect } from 'react';

const Cart = () => {
    useEffect(()=>{
              document.title="GlobalBazaar | Cart"
              },[]);
    return (
        <div>
            cart
        </div>
    );
};

export default Cart;